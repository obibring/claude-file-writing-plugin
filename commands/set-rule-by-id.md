---
name: set-rule-by-id
description: Update an existing rule by its ID (supports updating content, file types, or moving to new pathname)
allowed-tools:
  - Skill
argument-hint: "<id> [pathname] [file_types] [rule]"
---

# Set Rule By ID

Use the `managing-file-writing-rules` skill to update an existing rule.

The skill will handle interactive prompts, rule optimization, pathname moves, confirmation flows, and persistence.
