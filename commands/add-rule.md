---
name: add-rule
description: Create a new file writing rule scoped to a pathname and optional file types
allowed-tools:
  - Skill
argument-hint: "[pathname] [file_types] [rule]"
---

# Add File Writing Rule

Use the `managing-file-writing-rules` skill to add a new rule.

The skill will handle interactive prompts, rule optimization, validation, and persistence.
