---
name: get-rules
description: Retrieve file writing rules for a pathname with precedence ordering
allowed-tools:
  - Skill
argument-hint: "<pathname> [file_types] [max_age]"
---

# Get File Writing Rules

Use the `managing-file-writing-rules` skill to retrieve rules for the specified pathname.

The skill will handle querying, precedence ordering, filtering, and display.
